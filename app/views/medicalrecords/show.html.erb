<h4>Patient history</h4>
<table class="table table-striped table-bordered table-condensed">
    <tr><th>Record id</th><th>Doctor observation</th> <th>Prescription</th><th>Edit/delete</th></tr>
    <% @patient.medicalrecords.each_with_index do |c, i| %>
      <tr>
          <td><%= i+1 %></td>
          <td><%= c.doctor_observation %></td>
          <td> <%= c.prescription%></td>
          <td> <%= link_to 'edit',  edit_patient_medicalrecord_path(@patient, @medicalrecord)%>/<%= link_to 'delete', patient_medicalrecord_path(@patient, @medicalrecord),
                :confirm => 'Are you sure?', :method => :delete%></td>
      </tr>
    <% end %>
</table>
