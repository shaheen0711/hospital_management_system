

<h4>Choose a department, where you want to book appointment</h4>
<table class="table table-striped table-bordered table-condensed">
    <tr><th>id</th> <th>Department Name</th></tr>
    <% @departments.each do |department| %>


      <tr><td><%= department.id %></td>
          <td><%= link_to  department.department_name , department_detail_patient_path(department)%></td>
          </tr>

    <% end %>
    <tr>

    </tr>
</table>
<%= link_to "back", users_path %>